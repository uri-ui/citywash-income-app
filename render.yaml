services:
  - type: web
    name: citywash-income-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    routes:
      - path: /
        matchType: prefix
        dest: /
